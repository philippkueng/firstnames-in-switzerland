# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure("2") do |config|
  config.vm.box = "emacs-vagrant"
  config.vm.box_url = "http://pkstudio.ch/emacs-vagrant.box"
  config.vm.synced_folder ".", "/vagrant"
end
